$('#container').isotope({
  cellsByRow: {
    columnWidth: 422,
    rowHeight: 248
  }
});

// var clock = $('.clock').FlipClock({

// 	clock.start()
// });

// var clock  = $('.clock').FlipClock();

// var time = clock.setTime(3600);

var clock = $(".clock-chicken").FlipClock(3000, {
		clockFace: 'MinuteCounter'
	});

var clock = $(".clock-turkey").FlipClock(3000, {
		clockFace: 'MinuteCounter'
	});

var clock = $(".clock-duckling").FlipClock(3000, {
		clockFace: 'MinuteCounter'
	});

var clock = $(".clock-geese").FlipClock(3000, {
		clockFace: 'MinuteCounter'
	});

